rai:
  version: 0.2
resources:
  cpu:
    architecture: ppc64le
  network: true
commands:
  build_image:
    image_name: raiproject/scope:ppc64le-cuda92-latest
    dockerfile: "./Dockerfile" # the location of the Dockerfile on your local file system
    no_cache: true